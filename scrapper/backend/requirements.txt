# ──────────────────────────────────────────────────────────────
# Contractor Scraper Agent — Backend Dependencies
# Production-scraper aligned baseline + our additions
# ──────────────────────────────────────────────────────────────

# Core (from production scraper)
python-dotenv>=1.0.0
pydantic>=2.5.0
requests>=2.31.0
# psycopg2-binary removed — storage is now Google Sheets (see agent/sheets_client.py)

# Google Sheets storage
gspread>=6.1.0
google-auth>=2.30.0
# pandas removed — DBPR is now streamed + match-filtered (agent/dbpr_loader.py),
# no in-memory DataFrame.

# Cloud Run Jobs — the API service triggers the pipeline as a Cloud Run Job
# (PIPELINE_RUNNER=cloud_run_job). Not imported in thread mode.
google-cloud-run>=0.10.0

# Web framework + async
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.9

# Matching + normalization
rapidfuzz>=3.6.0

# Config
PyYAML>=6.0.1

# Auth — JWT + password hashing
PyJWT>=2.8.0
bcrypt>=4.1.0

# Excel export (.xlsx) — CSV uses the stdlib; xlsx needs openpyxl.
openpyxl>=3.1.0
